
apiVersion: batch/v1
kind: Job
metadata:
  name: mongo-test
spec:
  template:
    spec:
      containers:
      - name: test
        image: mongo:7
        command: ["mongosh", "--host=mongodb", "-u", "root", "-p", "root", "--eval", "db.runCommand({ ping: 1 })"]
      restartPolicy: Never
