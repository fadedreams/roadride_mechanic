# Connect to ES (use HTTP since security is disabled)
elasticsearchHosts: "http://elasticsearch-master:9200"

# Low resources
resources:
  requests:
    cpu: "100m"
    memory: "512Mi"
  limits:
    cpu: "500m"
    memory: "1024Mi"
