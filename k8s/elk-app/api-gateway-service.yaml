apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: roadride
spec:
  selector:
    io.kompose.service: api-gateway
  ports:
  - port: 8085
    targetPort: 8085
    nodePort: 30081
    protocol: TCP
  type: NodePort
