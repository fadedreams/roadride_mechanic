# k8s/api-gateway/configmap.yaml  (replace the old one)
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-config
data:
  CONSUL_ADDRESS: "http://consul:8500"
  JAEGER_ENDPOINT: "http://jaeger:4318/v1/traces"
  SERVICE_NAME: "api-gateway"
  SERVICE_PORT: "8085"
